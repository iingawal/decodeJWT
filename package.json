{
  "scripts": {
    "start": "ts-node src/decodejwt.ts",
    "build": "tsc --outDir dist",
    "start:prod": "node dist/decodejwt.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "jwt-decode": "^4.0.0",
    "prompt-sync": "^4.2.0"
  }
}
